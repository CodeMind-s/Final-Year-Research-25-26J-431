# Place your .pkl model file here

This directory should contain your trained demand/price prediction model.

Expected file: `demand_price_model.pkl`

Or update the `MODEL_PATH` environment variable in `.env` to point to your model file.
