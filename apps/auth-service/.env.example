NOTIFY_LK_USER_ID=your-notify-lk-user-id
NOTIFY_LK_API_KEY=your-notify-lk-api-key
NOTIFY_LK_SENDER_ID=NotifyDEMO
KAFKA_BROKER=localhost:29092
MONGO_URI=mongodb+srv://brinexAdmin:1no83DWF6n31kkj3@cluster0.tk0ipzf.mongodb.net/brinex?appName=Cluster0

# JWT signing — RS256 mode (preferred, required for Lab Agent / Phase 4):
#   generate with ./scripts/generate-jwt-keys.ps1, then set both keys here.
# Legacy HS256 mode (fallback): leave the two keys unset and use JWT_SECRET.
# JWT_SECRET=secret
# JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
# JWT_ISSUER=brinex-auth
# JWT_ACCESS_TTL=7d